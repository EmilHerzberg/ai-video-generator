# Provider API key(s) — the engine is provider-agnostic (Vercel AI SDK). Set the one you use.
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
# Optional: override the default model per provider, e.g. DEEPSEEK_MODEL=deepseek-v4-pro
# Dev preview URL the agent's QA inspector renders against (default http://localhost:5173)
PREVIEW_URL=http://localhost:5173
